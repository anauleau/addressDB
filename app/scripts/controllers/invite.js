'use strict';

/**
 * @ngdoc function
 * @name addressDbApp.controller:InviteCtrl
 * @description
 * # InviteCtrl
 * Controller of the addressDbApp
 */
angular.module('addressDbApp')
  .controller('InviteCtrl', function ($scope, invitation, Ref) {
      console.log(invitation);
  });
